import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: [
      "images.unsplash.com",
      "res.cloudinary.com",
      "cdn.pixabay.com",
      "i.scdn.co",
      "cdn.shopify.com",
    ]},
  }
export default nextConfig;
